const Footer = () => {
  return (
    <footer className="mt-8 text-center text-sm text-gray-500">
      <p>© {new Date().getFullYear()} Planning Médico - Sistema de gestión</p>
    </footer>
  );
};

export default Footer; 